/* ---------------------------------------------------------------------------
 * Committed default keys for the deployed app.
 *
 * Paste your key between the quotes below, then commit & push. It will apply to
 * everyone who opens the site, so nobody is prompted.
 *
 * ⚠️  This file IS public (it's in the repo and served to every visitor). Treat
 *     any key you put here as a BURNER — it will be scraped and possibly
 *     auto-revoked, so rotate it freely. Do NOT use a key you care about.
 *     (For a private/local machine, use the git-ignored config.local.js instead,
 *      which overrides this file — see config.local.example.js.)
 * ------------------------------------------------------------------------- */
window.VAN_KEYS = {
  ors: "",   // <-- paste your OpenRouteService key here
  nps: ""    // optional: US national-park campgrounds key
};
